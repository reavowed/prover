Define partial and total orders on a set

type strictTotalOrder < (A)
  format (on A)
  name (strict total order)
  definition (∧ ∧ ∧ relation < A asymmetricRelation < A transitiveRelation < A semicompleteRelation < A)
